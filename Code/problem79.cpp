#include <iostream>
#include <fstream>
#include <string>
#include <cmath>
#include <vector>
#include "math_unsigned.cpp"

int main ()
{
    //Has the text file p079_keylog.txt, which contains
    //Triples of digits from a number, where the digits are
    //randomly chosen but always stay in order
    //Find the smallest number which satisfies all of the lines

    //No code written, just examined file and it was easy enough
    //73162890
    return 0;
}